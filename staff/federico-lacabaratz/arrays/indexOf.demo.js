'use strict';

console.log('DEMO IndexOf');

var numbers = [3, 4, 67, 75, 32];

var num = 4;

console.log('If the number', num, 'exists within', numbers, 'it will show the index number, otherwise returns -1. In this case it will be:', indexOf(numbers, num));