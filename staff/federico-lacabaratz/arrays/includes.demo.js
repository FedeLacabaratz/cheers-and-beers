'use strict';

console.log('DEMO Includes');

var numbers = [32, 45, 2, 1, 56, 78];

var included = 78;

console.log('if my array', numbers, 'includes the number', included, ', then it should return true, in this case it will be:', includes(numbers, included));