'use strict';

console.log('DEMO Concat');

var a = ['a', 'b', 'c'];

var b = ['d', 'e', 'f'];

console.log('if', a, 'and', b, 'are to be merged');

console.log('it should return a new array =', concat(a, b));