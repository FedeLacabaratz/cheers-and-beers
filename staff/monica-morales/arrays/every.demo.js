'use strict';

/*console.log('DEMO every');
var numbers= [15,5,8,37,3,54,45];
function allSmallerThan(value){
    return value < 60;
};

console.log('if all elements in an arrary are < than 60  included within', numbers);
console.log('It should return true, atherwise false....the result on this case is', every(numbers, allSmallerThan));*/
