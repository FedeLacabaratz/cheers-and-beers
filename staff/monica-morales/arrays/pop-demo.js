'use strict';

console.log('DEMO pop');

var names = ['Jhon','Peter','Max', 'Jill','Alexandra'];
console.log('it',names);
console.log('should remove the last value', names.pop());

var numbers = [1,2,8,9,7,8,6,5];
console.log('it',numbers);
console.log('should remove the last value', numbers.pop());

var names = ['Jhon','Peter','Max', 'Jill','Alexandra'];
console.log('it',names);
console.log('the last value I can push in a variable');
var value_delete = names.pop();
console.log(value_delete);
 